@model IEnumerable<ProductionSystem.Web.Data.Entities.Produccion>

@{
    ViewData["Title"] = "ProduccionFinalizadaUsuarioAsync";
}

<link rel="stylesheet" href="https://cdn.datatables.net/1.10.19/css/jquery.dataTables.min.css" />
<br />

<h2>Producciones Finalizadas</h2>

<div class="row">
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="panel-body">
                <table class="table table-hover table-responsive-xl table-striped" id="ProduccionFinalizadaTable">
                    <thead>
                        <tr>

                            <th>
                                @Html.DisplayNameFor(model => model.FechaProduccion)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Resultado.Cantidad)
                            </th>


                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.FechaProduccion)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Resultado.Cantidad)
                            </td>

                            <td align="right">
                                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-primary">Details</a>
                            </td>
                        </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="//cdn.datatables.net/1.10.19/js/jquery.dataTables.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#ProduccionFinalizadaTable').DataTable();
        });
    </script>
}